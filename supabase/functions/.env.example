# Secrets for local Supabase Edge Function development.
# Copy this to .env and fill in real values (never commit .env).
# Set in production with: supabase secrets set ANTHROPIC_API_KEY=sk-ant-...

ANTHROPIC_API_KEY=sk-ant-REPLACE_ME
